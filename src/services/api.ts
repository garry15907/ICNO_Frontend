/**
 * @deprecated Import from `@/services/localEngineApi` instead.
 *
 * Backwards-compatible shim for older `@/services/api` imports. All
 * symbols here point at the local FastAPI engine — do NOT add cloud
 * endpoints here.
 */
export * from "./localEngineApi";