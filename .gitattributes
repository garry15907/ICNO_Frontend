# 모든 텍스트 파일의 줄바꿈을 LF로 정규화(Windows CRLF ↔ LF 노이즈 방지).
# Lovable/GitHub는 LF를 쓰므로 여기에 맞춘다.
* text=auto eol=lf

# 바이너리는 건드리지 않음
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.webp binary
*.woff binary
*.woff2 binary
*.ttf binary
